<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil de <%= user.user_name %></title>
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f5f5f5;
            padding: 20px;
        }
        h1 {
            font-size: 28px;
            color: #333;
        }
        .movie-list {
            margin-top: 20px;
        }
        .movie-item {
            background-color: #fff;
            padding: 15px;
            margin-bottom: 15px;
            border-radius: 5px;
            box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);
        }
        .movie-title {
            font-size: 20px;
            font-weight: bold;
        }
        .movie-rating {
            color: #0078d4;
        }
        .movie-opinion {
            font-style: italic;
            margin-top: 5px;
        }
    </style>
</head>
<body>
<h1>Perfil de <%= user.user_name %></h1>
<h2>Películas que te gustan:</h2>

<div class="movie-list">
    <% movieUserList.forEach(function(movie) { %>
        <div class="movie-item">
            <div class="movie-title"><%= movie.title %></div>
            <div class="movie-rating">Puntuación: <%= movie.rating %>/10</div>
            <div class="movie-opinion"><%= movie.opinion %></div> <!-- Asegúrate de tener esta línea si necesitas mostrar la opinión -->
        </div>
    <% }); %>
</div>
